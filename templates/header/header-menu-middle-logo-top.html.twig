<header class="fixed-top scroll-change" data-menu-anima="fade-in">
    <div class="navbar navbar-default navbar-fixed-top" role="navigation">
        <div class="navbar-main navbar-middle mega-menu-fullwidth">
            <div class="container">
                <div class="scroll-hide">
                    <div class="container">
                        <a class="navbar-brand center" href="{{ path('<front>') }}">
                            <img src="{{ logo_image_header_6 }}" alt="logo" />
                        </a>
                    </div>
                </div>
                {% if page.header %}
                    {{ page.header }}
                {% endif %}
                <div class="collapse navbar-collapse minimal-header-middle-logo-top">
                    {% if page.main_menu %}
                        {{ page.main_menu }}
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</header>