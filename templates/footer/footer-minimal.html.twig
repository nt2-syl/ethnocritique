<footer class="footer-minimal">
    <div class="content">
        <div class="container">
            {% if page.footer_minimal %}
                {{ page.footer_minimal }}
            {% endif %}
            {% if copyright_text %}
            <div class="row copy-row">
                <div class="col-md-12 copy-text">
                    <div class="tag-row">
                        {{ copyright_text|t }}
                    </div>
                </div>
            </div>
            {% endif %}
        </div>
    </div>
</footer>